-- data binding ==> one way ==> {{}} interpolation
                            ==> [] property binding
                            ==> () event binding
                ==> two way binding ==> [] + () = [(ngModel)] ==> banana in the box           